<div class="max-w-sm rounded overflow-hidden shadow-lg bg-white">
  <div class="px-6 py-4">

    <div>
      This playlist was created xxxxx days ago
    </div>
    
      {{#each playlistAll as |playlist|}}
      <div>

        <h3> Test
          {{playlist.id}}
          {{playlist.playlist_name}} 
        </h3>

      </div>
      {{/each}}
      <div>
        <p> Test </p>
      </div>
    </ul>
  </div>
</div>

